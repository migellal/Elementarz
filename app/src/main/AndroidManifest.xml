<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.e_lementarz.elementarz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MenuActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.words.WordsActivity"
            android:label="@string/app_name"
            android:parentActivityName=".activities.MenuActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.MenuActivity" />
        </activity>
        <activity
            android:name=".activities.numbers.mainly.NumbersActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.MenuActivity" />
        </activity>
        <activity
            android:name=".activities.numbers.BricksActivity"
            android:label="@string/app_name"
            android:parentActivityName=".activities.numbers.mainly.NumbersActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.numbers.mainly.NumbersActivity" />
        </activity>
        <activity
            android:name=".activities.numbers.CompareActivity"
            android:label="@string/app_name"
            android:parentActivityName=".activities.numbers.mainly.NumbersActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.numbers.mainly.NumbersActivity" />
        </activity>
        <activity
            android:name=".activities.numbers.BricksOrderActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.numbers.mainly.NumbersActivity" />
        </activity>
        <activity
            android:name=".activities.numbers.CompareCheckActivity"
            android:label="@string/app_name"
            android:parentActivityName=".activities.numbers.mainly.NumbersActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.numbers.mainly.NumbersActivity" />
        </activity>
        <activity
            android:name=".activities.numbers.AddingActivity"
            android:label="@string/app_name"
            android:parentActivityName=".activities.numbers.mainly.NumbersActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.numbers.mainly.NumbersActivity" />
        </activity>
        <activity
            android:name=".activities.numbers.AddingCheckActivity"
            android:label="@string/app_name"
            android:parentActivityName=".activities.numbers.mainly.NumbersActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.numbers.mainly.NumbersActivity" />
        </activity>
        <activity
            android:name=".activities.numbers.ChoiceActivity"
            android:label="@string/app_name"
            android:parentActivityName=".activities.numbers.mainly.NumbersActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.numbers.mainly.NumbersActivity" />
        </activity>
        <activity
            android:name=".activities.numbers.ChoiceCheckActivity"
            android:label="@string/app_name"
            android:parentActivityName=".activities.numbers.mainly.NumbersActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.numbers.mainly.NumbersActivity" />
        </activity>
        <activity
            android:name=".activities.numbers.mainly.NumbersPracticeActivity"
            android:label="@string/title_activity_numbers_practice"
            android:parentActivityName=".activities.MenuActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="org.e_lementarz.elementarz.activities.MenuActivity" />
        </activity>
        <activity
            android:name=".activities.numbers.mainly.NumbersTestActivity"
            android:label="@string/title_activity_numbers_test"
            android:parentActivityName=".activities.MenuActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="org.e_lementarz.elementarz.activities.MenuActivity" />
        </activity>
        <activity
            android:name=".activities.numbers.mainly.NumbersResultActivity"
            android:label="@string/title_activity_numbers_result"
            android:parentActivityName=".activities.MenuActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="org.e_lementarz.elementarz.activities.MenuActivity" />
        </activity>
    </application>

</manifest>
<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".activities.NumbersActivity"
    tools:showIn="@layout/activity_numbers">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/numbersBricksLbl"
        android:id="@+id/numbersBricksBtn"
        android:onClick="onClickNumbers"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/numbersCompareLbl"
        android:id="@+id/numbersCompareBtn"
        android:onClick="onClickNumbers"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@+id/numbersOrderBtn" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/order"
        android:id="@+id/numbersOrderBtn"
        android:onClick="onClickNumbers"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@+id/numbersBricksBtn" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/compareCheck"
        android:id="@+id/numbersCompareCheckBtn"
        android:onClick="onClickNumbers"
        android:layout_alignTop="@+id/numbersCompareBtn"
        android:layout_toEndOf="@+id/numbersCompareBtn" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/adding_bricks"
        android:id="@+id/numbersAddingBricksBtn"
        android:onClick="onClickNumbers"
        android:layout_below="@+id/numbersBricksBtn"
        android:layout_alignParentStart="true" />

</RelativeLayout>
